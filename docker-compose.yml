services:

  postgres:
    image: postgres:13.1
    restart: always
    environment:
      POSTGRES_PASSWORD: '${DB_PASSWORD:-example}'
    ports:
      - '${DB_PORT:-5432}:5432'

  adminer:
    image: adminer
    restart: always
    ports:
      - '${ADMINER_PORT:-8080}:8080'
